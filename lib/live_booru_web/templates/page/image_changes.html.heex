<div class="content">

<h1>Changes for <%= live_redirect(@image.id, to: Routes.live_path(@socket, LiveBooruWeb.ImageLive, @image.id)) %></h1>

<table>
  <tr>
    <th>Date</th>
    <th>User</th>
    <th>Tags</th>
    <th>Source</th>
  </tr>
  <%= for change <- @changes do %>
  <tr>
    <td><%= change.inserted_at %></td>
    <td><%= live_redirect(change.user.name, to: Routes.live_path(@socket, LiveBooruWeb.UserLive, change.user.id)) %></td>
    <td><%= change.tags |> Enum.sort() |> Enum.join(", ") %></td>
    <td><%= change.source %></td>
  </tr>
  <% end %>
</table>

</div>
